import { abs } from "std:math";
import { time } from "std:time";

export function main(): i32 {
    clib.puts("Testing math:");
    let a: i32 = abs(-10);
    clib.puts("abs(-10) called");

    clib.puts("Testing time:");
    let now: i64 = time();
    clib.puts("time() called");

    return 0;
}
